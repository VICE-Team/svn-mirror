# vim: set noet ts=8 sw=8 sts=8:

if NATIVE_GTK3_COMPILE

glsldir = $(VICE_DATADIR)/GLSL

glsl_DATA = viewport.vert \
		  builtin.frag \
	      builtin-interlaced.frag \
	      bicubic.frag \
		  bicubic-interlaced.frag

EXTRA_DIST = $(glsl_DATA)

endif
